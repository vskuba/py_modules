# Зависимости py_modules: картинки и пиксельные замеры (adb_, ai_vision без LLM).
# Ядро тощее и остаётся таковым сознательно: снимок экрана работает без LLM-стека.
# В venv проекта — одна строка: -r py_modules/requirements.txt
Pillow>=11
numpy>=2.1
