# Тон картинок: чем мерить яркость фона и чем ровнять пачку по эталону

> Яркость подложки картинок мерится модой цвета в окне (`image_measure`) и
> подгоняется кусочно-линейными уровнями к эталону (`image_match`).
> Инструмент — `py_modules/image_` (ядро, Pillow), CLI: `python -m image_.image_`.
> Всё про измеренные числа и грабли замера; правки пикселей ради пикселей тут нет.

## 1. Замер: фон — это мода, а не среднее

`image_measure(path, box)` возвращает моду цвета (самую частную краску),
её яркость Rec.601 и среднюю по окну. Порядок чисел такой, потому что:

- **Среднее по кадру врёт.** Белые карточки и тёмный текст тянут среднее в
  стороны от подложки; на пачке страниц со скруглёнными карточками разница
  среднего больше, чем сам сдвиг фона.
- **Мода ищет подложку честно:** её в кадре больше всего. Но окно замера
  долями кадра (`box=(x0, y0, x1, y1)`) обязана лежать на чистом фоне:
  мода, снятая по всему кадру страницы с заголовком, иногда собирает цвет
  текста-тени; полоска у верхнего края правой половины экрана
  (`(0.55, 0.015, 0.95, 0.06)`) заведомо без букв.

Две грабли из живого прогона, обе молчаливые:

1. **Моду искать по всему диапазону 0–255.** Ограничение «ищем светлую
   подложку, floor 128» пропустило фон с настоящей модой 108: тёмный кадр
   съехался к 186 вместо 212, потому что LUT строился от выдуманной точки.
2. **Снимки сравнивать только внутри одного устройства и версии Android:**
   панели разные, одна и та же страница на телефоне и эмуляторе лежит на
   кадре разной высоты (`mobile_emulator.md`, §3).

`image_measure` дополнительно возвращает `spread` — размах p95−p5 яркости в
окне: на чистом фоне он единицы, большой размах означает, что окно захватило
посторонний объект. С `top=N` (флаг `--top`) приходит список из N самых
частых красок с числами — по нему видно, что в окне второй цветовой кластер,
которого мода не показывает.

## 2. Правка: уровни с якорями, а не «прибавить яркости»

`image_match(path, target)` строит кусочно-линейную кривую с тремя якорями:
`0 → 0`, `фон → цель`, `255 → 255`. Чёрный закреплён — текст не сливается с
фоном и контраст «тёмное по светлому» сохраняется; белый закреплён — света
не клиппуются. Это единственная правка, которая делает картинку «такой же
яркой», не делая её другой по цвету; `brightness`/`gamma` сдвигают всё,
включая текст.

Грабли:

1. **`Image.point` на RGB требует ровно 768 записей** — 256, повторённые
   трижды; на 256 Pillow бросает `wrong number of lut entries`.
2. **JPEG по умолчанию пишется с качеством 75** — цельноэкранная графика
   вокруг текста покрывается муаром. В `image_match` стоит `IMAGE_JPEG_QUALITY = 95`.
3. **Прозрачность сохраняется, но не всякая.** Уровни накладываются только на
   RGB-каналы, альфа RGBA-файла уходит в результат нетронутой (у проверенного
   прогона альфа совпадает побайтно). Но у JPEG альфы нет физически: сохранение
   в `.jpg` её выбрасывает — это ограничение формата, а не баг инструмента;
   индексируемые картинки с прозрачностью (`P` + `tRNS`) сплющиваются в RGB.

## 3. Идемпотентность: править всегда от оригинала

Профиль уровней, применённый второй раз, складывается с первым: «ещё чуть
ярче» превращается в выбитую подложку. `image_match(..., backup='каталог')`
запоминает первый встреченный файл нетронутым и правит всегда от него:
повторный прогон повторяет первый, а не накручивает. Прогон по уже
исправленным файлам без `backup` — верный способ всё испортить; на это
указывает и поле `changed: False` (уже в допуске ±1).

## 4. Аудит: репрезентативно ли окно замера

`image_audit(path)` проверяет фон кадра до того, как по нему что-то подгоняют:
девять зон сетки 3×3, четыре узкие полоски по кромкам (2% кадра, с отступом,
чтобы не захватить скруглённые углы) и вердикт — группы яркости по всем
областям (кластеры с разницей до `IMAGE_AUDIT_FLAG`). Кластеров больше одного —
предупреждение: фон не единый, и окно замера надо брать внутри той области,
под которую подгоняешь, а не «сверху у края».

Классический промах, который это ловит: фон страницы ярче фона карточки,
замер «у верхнего края» возвращает яркость страницы, и карточка подгоняется
под чужой тон — на экране появляется светлое пятно с видимым краем.
Медиана по зонам тут наврёт: в кадре с двумя фонами она примет один из них
и будет выглядеть правдоподобно, поэтому вердикт — именно группировка.

Тонкие полоски по кромкам — не для красоты: светлая полоса в 4% по верху
картинки в зону-треть не попадает вовсе, её замечает только кромка.

## 5. Шов: видно ли заплатку краем на живом экране

`image_seam(path, pos)` смотрит профиль яркости через предполагаемую границу:
медиана яркости прямоугольных срезов по пикселю, `span` клеток с каждой
стороны. Вердикт решает ступенька яркости вблизи самой границы
(±`IMAGE_SEAM_WINDOW`), разница медиан половинок — `delta`. Ступеньку ищут
только у границы, потому что буквы на заплатке и вокруг неё тоже делают
большие ступени, но швом не являются; они остаются в `profile_max` и
`profile` для отладки.

Полосу (`at`) выбирают там, где нет глифов, и проверку делают несколько раз
на разных `at` — настоящая ступень не уходит, куда её ни перенеси, а текстовая
замерает на месте. Режим `best=True` (`--best`) перебирает эти `at` сам:
положения полосы идут поперёк оси с шагом не меньше её толщины
(`IMAGE_SEAM_SCAN` проб), вердикт выносится по самой спокойной позиции,
а `agree`/`scanned` показывают стойкость — сколько проб согласны с вердиктом.
На тексте ступень завышается, и один неудачный `at` объявил бы шов там, где
его нет; `--best` снимает ручной выбор совсем.
Координаты берут из того же снимка экрана; если граница —
край DOM-элемента, экранные прямоугольники даёт `adb_cdp_element_rect`
(`mobile_control.md`, §8).

## 6. Все четыре стороны прямоугольника: `image_rect_seams`

Одной границы мало: плоский CSS-слой невидим, только когда стык не читается
ни с одной из четырёх сторон, а четыре вызова `seam` с ручным счётом
координат сторон — это способ считать не туда. `image_rect_seams(path, rect)`
берёт прямоугольник (запись `screen` из `adb_cdp_element_rect` или кортеж
`x,y,w,h`) и прогоняет каждую сторону как `seam --best`, полосу ища внутри
прямоугольника (с отступом от углов: за ним меряет уже соседний слой).
Сторона, лежащая ровно на краю кадра, помечается `skip` — сравнивать там
не с чем. Вердикт — `ok`: True, когда ни один край не читается краем.

## 7. Пара кадров: `image_diff_zones`

«Починили контраст» обычно меряют двумя вызовами `image_measure` и вычитают
числа в голове — так же легко вычесть не то окно, как и не туда.
`image_diff_zones(path_a, path_b)` обходит сетку (доли кадра, как в аудите)
сразу на двух кадрах и отдаёт по каждой зоне яркости мод и вердикт «не как
в эталоне» (та же ступень, `IMAGE_AUDIT_FLAG`). Окна — доли каждого кадра,
поэтому кадр другой высоты (свой статус-бар) не собьёт координатой; но доли
считают по каждому кадру свою арифметику — потому и предупреждение, в каких
зонах кадры различаются, чтобы окно замера брали внутри своей области.

## 8. Инвентарь цветов разметки: `image_literals`

Тоновый контракт обязывает после каждой тоновой правки картинок перемерять
все тоновые литералы CSS — до этого это grep по файлу и глаза.
`image_literals(path)` читает HTML/CSS как текст и выдаёт ведомость: каждый
уникальный цвет (hex 3/4/6/8 знаков без альфы, `rgb()`/`rgba()` сведены к
ним же) с яркостью Rec.601 и числом встреч. Рядом с `image_measure` по
кадру это отвечает на «что заявлено в CSS и не уехал ли тон» одним взглядом.

## 9. Обводка: где рамка, сколько пикселей и каким цветом — `image_stroke`

Нарисованную рамку (картинку с обведённым объектом) подводят в CSS: «два
пикселя `#646464`, угол двадцать». `image_stroke(path, rect)` меряет штрих
по всем четырём сторонам прямоугольника: положение центра ядра и сдвиг от
данной стороны, ширину ядра, цвет (моду ядра), яркость — и подсказку в CSS
долями ширины вьюпорта (`--rect`, запись `screen` из `adb_cdp_element_rect`;
без него — рамка по краю кадра).

Как ищет и почему именно так:

- **Профиль яркости каждого сечения — медиана поперёк полосы**
  (`IMAGE_STROKE_BAND`): глиф текста, попавший в сечение, один-два пикселя,
  медиана его выбрасывает; среднее уехало бы к цвету букв.
- **Линия ищется отклонением от местного фона, а не «темнее»**
  (`IMAGE_STROKE_CONTRAST`): рамка бывает и светлой (на тёмном фото);
  порог переживает и тёмный штрих, и светлый.
- **Сдвиг центра законен до половины ширины штриха:** и PIL, и CSS
  `border` рисуют границу *внутрь* номинального края, так что ядро 2 px на
  стороне 100 лежит на 100–101 с центром 100.5 — offset это и покажет.
- **Радиус угла — посадкой точек дуги на окружность, а не максимальным
  уходом центра.** У самого угла штрих стоит вертикально, медианного сечения
  там нет, и максимум недооценил бы радиус примерно вдвое; центр дуги живёт
  в `(r, r)` от угла — одна степень свободы, перебор `r` до `pad` минимизирует
  невязку. Потолок оценки — `pad`: радиус, съеденный полем, не виден вовсе.
  Углы слияния с соседней стороной отфильтрованы по ширине ядра.

## 10. Перенос тона между кадрами: `image_expose`

`image_match` делает кадр *таким же*, как эталон. Обратный вопрос возникает,
когда тон уже поехал по своей причине (другая экспозиция, другой прогон):
«этот hex из CSS — каким он станет на тонированном кадре?».
`image_expose(path_a, path_b, pairs)` строит перенос по окнам-референсам
(`(x0, y0, x1, y1)` долями кадра, одни и те же в обоих кадрах): по каждой
канальной паре значений — кусочно-линейная кривая с якорями 0 и 255,
цвета для переноса (`colors=`) прогоняются сквозь неё. Усилитель (`gains`)
считается по самому тёмному референсу: на светлых концах кривая чаще
упирается в клиппинг, и именно об этом говорит `residual` — максимальное
отклонение предсказания от факта на референсах.

Две ловушки, обе подняты в ошибку: неразличимые по яркости референсы
(перенос не построится) и непонятный канал (нужен хотя бы один референс,
иначе нечего интерполировать).

## 11. Тоновый контракт разметки: `image_contract`

Правило проекта: каждая hex-литерала рукописной разметки скопирована с
картинки, а комментарий рядом называет файл и окно замера. Слова в
комментарии не проверяемы — `image_contract(path)` проверяет их машинно:
для каждого литерала ищет ближайший комментарий (в пределах
`IMAGE_CONTRACT_GAP` знаков) с именем картинки и окном долями кадра, грузит
картинку (рядом с разметкой, в `public/images/`, `public/`, `images/`) и
сверяет с модой окна. Статусы: `ok`, `drifted` (расхождение канала больше
`IMAGE_CONTRACT_TOL` — тон уехал после правки картинок), `unattributed`
(нет ни файла, ни окна — рукой не проверить) и `missing` (картинки нет на
месте). Литералы внутри комментариев не считаются: это цитаты («было
#aabbcc»), а не объявления.

Смысл не в зелёном выводе, а в списке нарушений: именно `drifted` — это
забытая после `image_match` перемерка, а `unattributed` — литерал, у которого
через месяц никто не вспомнит происхождение.

## 12. Хотспоты: пиксели ↔ доли кадра — `image_frac`

Арифметика хотспотов (`(y − 91) / 2186`) в каждой сборке считается вручную.
`image_frac(path, rect=…|box=…, crop=…)` переводит в обе стороны: `rect`
(x, y, w, h пикселей целого кадра, та же запись `screen`) — в доли, `box`
(доли) — в пиксели. `crop` — **PIL-box вырезаемого окна целого кадра, та же
запись, что держит `.crop(...)` в конвейере** (`0,91,1080,2277`), а не
отступы со сторон: цифры в проекте хранятся уже в этой форме, и второй
формой можно только ошибиться. Отдаёт `px_cropped` (координаты в кропнутом
кадре — именно от них считается доля), `frac` (до 4 знаков, как в разметке),
`vw` (те же числа в CSS-единицах — проценты **ширины** cropped-кадра для x, y,
w и h: по мобильной конвенции в vw задают и `top`, и `height`, чтобы вёрстка
масштабировалась пропорционально; `vw.x` при этом не равен `frac.x0`, а
`px.x / ширину × 100`) и `px` (для box — в целых пикселях кадра).

## CLI

```bash
python -m image_.image_ measure page.png --box 0.55,0.015,0.95,0.06
python -m image_.image_ measure page.png --top 5
python -m image_.image_ audit card.png
python -m image_.image_ seam shot.png --pos 362 --at 690
python -m image_.image_ seam shot.png --pos 655 --at 232 --axis y
python -m image_.image_ seam shot.png --pos 908 --best   # полосу ищет сам
python -m image_.image_ rect-seams shot.png --rect 908,144,99,115
python -m image_.image_ diff original.jpg shot.png
python -m image_.image_ literals www/document.html
python -m image_.image_ match a.png b.jpg --target ref.jpg --backup orig/ --box ...
python -m image_.image_ stroke framed.png --rect 908,144,99,115 --pad 30
python -m image_.image_ expose before.png after.png --pair 0,0,0.2,0.2 --pair 0.4,0.4,0.6,0.6 --color 114,113,105
python -m image_.image_ contract www/reserve.html
python -m image_.image_ frac shot.png --rect 66,290,949,1542 --crop 0,91,1080,2277
python -m image_.image_ frac shot.png --box 0.0611,0.0914,0.9407,0.7991 --crop 0,91,1080,2277
```

`--target` — файл эталона (берётся яркость его фона) или число 0–255;
`--out` — каталог, иначе поверх; вывод построчный: `было → стало (цель,
записано|уже в допуске) → файл`.
